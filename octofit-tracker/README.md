OctoFit Tracker — project scaffold

Structure:

octofit-tracker/
├── backend/
│   ├── venv/            # Python virtual environment (ignored)
│   └── octofit_tracker/ # Django app package (placeholder)
└── frontend/            # React / web frontend (placeholder)

Quickstart (local, from workspace root):

1. Create venv:

```bash
python3 -m venv octofit-tracker/backend/venv
source octofit-tracker/backend/venv/bin/activate
pip install -r octofit-tracker/backend/requirements.txt
```

2. Follow Django setup steps to create project and apps.
